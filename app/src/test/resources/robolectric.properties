# Robolectric doesn't support API 37 yet — pin its simulated SDK to the newest it supports
sdk=36
# Run every Robolectric test against HiltTestApplication so the real Application class
# (Hilt + Firebase init) never executes on the JVM
application=dagger.hilt.android.testing.HiltTestApplication
